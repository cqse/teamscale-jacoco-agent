plugins {
	id 'com.teamscale.java-convention'
}

version 'unspecified'

dependencies {
	testImplementation(libs.junit4)
}

jar {
	manifest {
		attributes(
				'Main-Class': 'com.example.Main'
		)
	}
}