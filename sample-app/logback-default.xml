<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <define name="defaultLogDir" class="com.teamscale.jacoco.agent.util.LogDirectoryPropertyDefiner" />

    <appender name="logstash" class="shadow.net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>11f59d5b-61a8-4816-af08-1bab248a70e4-ls.logit.io:19674</destination>
        <encoder class="shadow.net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"Customer-specific field": "crazy value", "hostname": "${HOSTNAME}"}</customFields>
        </encoder>
    </appender>

    <appender name="RollingFile" class="shadow.ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- We write to a file that is located in a folder provided via the context -->
        <file>/home/k/proj/teamscale-jacoco-agent/sample-app/agent.log</file>

        <rollingPolicy class="shadow.ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>${defaultLogDir}/teamscale-jacoco-agent-%i.log.zip</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>10</maxIndex>
        </rollingPolicy>

        <triggeringPolicy class="shadow.ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>1MB</maxFileSize>
        </triggeringPolicy>

        <encoder>
            <pattern>%date{yyyy-MM-dd HH:mm:ss} %-5level %logger{35} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="debug">
        <appender-ref ref="RollingFile"/>
        <appender-ref ref="logstash"/>
    </root>

    <logger name="shadow.org.apache.spark" level="warn" />
    <logger name="shadow.org.eclipse.jetty" level="warn" />
</configuration>
